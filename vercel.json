{
    "version": 2,
    "builds": [
        {
            "src": "pyapi/**/*.py",
            "use": "@vercel/python"
        },
        {
            "src": "server/api/*",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "/pyapi/(.*)",
            "dest": "/pyapi/$1.py"
        },
        {
            "src": "/api/(.*)",
            "dest": "/server/api/$1"
        }
    ]
}