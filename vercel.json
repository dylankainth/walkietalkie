{
    "version": 2,
    "builds": [
        {
            "src": "pyapi/**/*.py",
            "use": "@vercel/python"
        }
    ],
    "routes": [
        {
            "src": "/pyapi/(.*)",
            "dest": "/pyapi/$1.py"
        }
    ]
}